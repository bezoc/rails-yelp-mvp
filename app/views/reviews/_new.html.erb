<%= simple_form_for [restaurant, review] do |f| %>
  <%= f.input :rating, label: "Rating (0-5)" %>
  <%= f.input :content, label: "Write your review" %>
  <%= f.button :submit, "Save review", class: "btn btn-primary mt-2" %>
<% end %>
